library(rlR)
env = makeGymEnv("CartPole-v1")
env$overview()
listAvailAgent(env)
conf = getDefaultConf("AgentFDQN")
conf$show()
conf$set(render = TRUE, console = TRUE, policy.maxEpsilon = 0.1)
agent = makeAgent("AgentFDQN", env, conf)
perf = agent$learn(200L)
perf$plot()
