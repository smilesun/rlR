Environment = R6Class("Environment",
  public = list(
    env = NULL,
    act_cnt = NULL,
    state_cnt = NULL,
    state_dim = NULL,

    initialize = function(env) {
    },
    step = function(action) {
    }
    ),
  private = list(),
  active = list()
  )

